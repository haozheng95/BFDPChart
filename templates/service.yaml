apiVersion: v1
kind: Service
metadata:
  name: {{ include "BFDPchart.fullname" . }}
  labels:
    {{- include "BFDPchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "BFDPchart.selectorLabels" . | nindent 4 }}
